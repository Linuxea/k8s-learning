# 用于验证集群是否正常工作的测试 Pod
apiVersion: v1
kind: Pod
metadata:
  name: verify-nginx
  labels:
    app: verify-nginx
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      ports:
        - containerPort: 80
