apiVersion: v1
kind: Service
metadata:
  name: {{ include "hook-demo-chart.fullname" . }}
  labels:
    {{- include "hook-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hook-demo-chart.selectorLabels" . | nindent 4 }}
